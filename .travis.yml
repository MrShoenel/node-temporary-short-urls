language: node_js

before_install:
  - npm install -g grunt
  - npm install -g grunt-cli
  - npm install -g grunt-ts
  - npm install -g matchdep
  - npm install -g mocha
  - npm install

before_script: grunt default

script: mocha

node_js:
  - "5.3"
